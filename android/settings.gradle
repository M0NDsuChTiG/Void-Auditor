// Native-only: Capacitor-каркас (capacitor-android/haptics/cordova-plugins) не используется
// приложением (:app не имеет на него зависимостей) и ломал сборку unqualified-задач
// («invalid source release: 21»). Приложение — чистый Compose + Shizuku.
include ':app'
