# VOID Auditor — Отчёт по NET_SCAN

**Версия:** 1.4.0 (code 4) · **Дата:** 09.08.2026  
**Устройства тестирования:**
- Samsung SM-A115F (`R9HN50GZL4J`, Android 11) — debug APK (SKIP_BIOMETRIC=true), release APK
- ChromeOS (`13.13.213.117:5555`) — debug APK

---

## 1. Задача

Вкладка `NET_SCAN`:
1. Полный скан всех портов 1–65535 (по требованию пользователя).
2. Отмена скана (кнопка `CANCEL`).
3. Больше информации о найденном хосте: открытые порты + определение сервисов (баннеры).
4. Скорость: полный скан не должен занимать «часы».

## 2. Реализация

### `network/NetworkScanner.kt`
- `COMMON_PORTS` — без дублей: `22, 80, 443, 5555, 8080, 8443, 9090, 3389, 5900`.
- `FULL_PORTS = (1..65535)` — режим «все порты».
- Таймаут TCP-connect: **800 мс → 350 мс** (LAN-диапазон). Скорость ~2.3×.
- Параллельность: партии по 256 портов, `limitedParallelism(256)`.
- **Баннер-детект сервисов** на открытых портах (`grabService`):
  - HTTP-порты (80, 8000, 8080, 8888, 3000, 5000, 8081, 9080): шлётся `GET / HTTP/1.0` → считывается первая строка ответа;
  - остальные порты: чтение приветственного баннера (SSH/SMTP/FTP/…);
  - санайт-строка: только печатные символы, максимум 64 символа;
  - если баннер пустой — подставляется известное имя порта (`53 → DNS`, `22 → SSH`, `5555 → ADB` и т.д.).
- Отмена: полный `CancellationException` проброс через `coroutineScope` → `scanJob.cancel()`.

### `network/NetworkProfile.kt`
- `HostInfo` расширен полем `services: Map<Int, String>` (порт → баннер/имя сервиса).

### `network/NetworkDashboard.kt`
- Переключатель `COMMON PORTS` / `ALL 1-65535`, кнопка `CANCEL` во время скана.
- Двухуровневый прогресс: `host 1/5 • port 45000/65535`.
- Live-появление хостов по мере завершения каждого (`onHostResult`).
- Карточка хоста: `Open: 22, 5555` + строка сервисов `22: SSH-2.0-OpenSSH_8.4p1`.
- Диалог хоста при тапе: HOSTNAME, MAC, VENDOR, RTT, ALIVE, OPEN PORTS, **SERVICES** — каждая строка с кнопкой копирования, плюс `COPY ALL (JSON)` (включая `"services":{...}`) и `DISMISS`.
- Копирование логируется в консоль: `COPIED KEY → value`.

## 3. Результаты измерений (телефон, сеть 13.13.213.0/24)

| Параметр | Было (800 мс) | Стало (350 мс) |
|---|---|---|
| Полный скан портов на 1 хост | ~4–5 мин | ~2.5–3 мин |
| Скан COMMON (9 портов) на хост | ~25 с | ~8–10 с |
| Live-детект первого хоста | порог хост-за-ход | каждый хост сразу по завершении |

Наблюдение из логов прогресса: `host 1/5 • port 44296/65535` → `host 2/5 • port 13014/65535` — прогресс корректно живёт между хостами.

Найденные хосты в тестовой сети: `13.13.213.115` (сам телефон), `13.13.213.111` (MoNDsuChTiG.lan, MAC `18:31:BF:53:5C:EC`).

## 4. Проверено в UI (uiautomator dump)

- [x] Диалог хоста: 6 строк данных + `SERVICES` + `COPY ALL (JSON)` + `DISMISS`
- [x] Кнопки копирования с `content-desc="copy <KEY>"`
- [x] Живой прогресс портов, `CANCEL` активен во время скана
- [x] Live-добавление хостов без перезапуска скана
- [x] Режимы COMMON/ALL переключаются (чип запрещается менять во время скана)

## 5. Наблюдения / замечания

1. **Баннеры**: у `13.13.213.111` открытый порт `53` — при отсутствии текстового баннера отображается имя сервиса из справочника (`DNS`), не «пустая строка».
2. **Собственный IP** попадает в результаты скана (ping самого себя возвращается) — ожидаемо, не баг.
3. **Full-скан 5 хостов** суммарно ~12–15 мин — терпимо для анализа, но и заявки на ускорение в проде: уменьшать тайминг ниже 300 мс рискованно (потеря медленных устройств в WiFi).
4. **Биометрия**: release-сборка требует биометрическую аутентификацию; для автоматических тестов по adb используется debug-сборка (`SKIP_BIOMETRIC=true`).
5. GitHub не затрагивался — фича не коммичена, ждёт финального подтверждения пользователя.

## 6. Файлы изменений

- `android/app/src/main/java/com/kuzyamond/voidauditor/network/NetworkScanner.kt`
- `android/app/src/main/java/com/kuzyamond/voidauditor/network/NetworkProfile.kt`
- `android/app/src/main/java/com/kuzyamond/voidauditor/network/NetworkDashboard.kt`